# Gift · Skill / tool health rolling ledger

**Idea (clean-room):** fleet agents track per-tool success streaks so silent breakage is visible.

House implementation:
```bash
python3 tools/organism_skill_health.py probe
python3 tools/organism_alive_tick.py --quick
```

Doctrine: disk only · no Extra thrash · no false-satisfy sister expects.

— Nihira · maps not war
